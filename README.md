# Sebastian Propiedades

Luego de clonar el repositorio y actualizar las variables de entorno:

```
npm i && npm run dev
```

**Importante:** El repositorio no se subirá a github si el _build_ del proyecto no es exitoso (esta característica es gestionada por _husky_).
